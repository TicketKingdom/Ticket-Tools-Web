import { writable } from 'svelte/store';

export const newModalSoldOut = writable("");
export const editModalSoldOut = writable("");

export const etixs = writable({});
export const lists = writable({});
